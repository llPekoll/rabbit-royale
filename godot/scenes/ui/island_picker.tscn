[gd_scene load_steps=2 format=3]

; QUELLE ILE ? — voir scripts/ui/island_picker.gd. Un Dialog ; il demande
; lui-meme le listing a RunState et emet `chosen(choice)`.

[ext_resource type="Script" path="res://scripts/ui/island_picker.gd" id="1_picker"]

[node name="IslandPicker" type="Control"]
layout_mode = 3
script = ExtResource("1_picker")
