[gd_scene format=3]

; Le panneau du joueur — voir scripts/ui/profile.gd. Le cadre, le titre et le
; [x] sont construits par Dialog dans _init ; la scene ne porte que le script.

[ext_resource type="Script" path="res://scripts/ui/profile.gd" id="1_profile"]

[node name="Profile" type="Control"]
script = ExtResource("1_profile")
