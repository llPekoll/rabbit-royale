[gd_scene format=3]

; SCENE VIDE pour verify_crossing.gd : un lieu sans contenu, mais avec le
; piege qui fuyait — un CanvasLayer et un bouton, que la visibilite du parent
; n'atteint pas.

[node name="Empty_doorstep" type="Control"]

[node name="Layer" type="CanvasLayer" parent="."]
layer = 10

[node name="Button" type="Button" parent="Layer"]
text = "doorstep"
