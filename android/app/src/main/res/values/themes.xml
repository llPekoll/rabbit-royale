<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Fond identique a la variable bg du site : sans ca, un flash blanc apparaît entre
         le lancement de l'activité et le premier paint de la WebView. -->
    <style name="Theme.RabbitRoyale" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:navigationBarColor">@color/bg</item>
    </style>
</resources>
